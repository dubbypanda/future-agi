P0: none found.

P1: `EvalPlayGroundAPIView` CH context reads are still not tenant-scoped. `org` is available, but span/trace/session auto-context resolution reads by raw ID with no `project__organization` equivalent: [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5661), [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5703), [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5716), [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5797). This appears equivalent to the old unscoped ORM path, but it fails the tenant-scope bar in the review prompt.

P2: Session CH aggregation no longer preserves the ORM FK join to non-deleted traces. The code builds `_trace_qs = Trace.objects.filter(session=_ss, deleted=False)`, but totals are computed from `reader.list_by_session()` before intersecting with those trace IDs: [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5800), [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5813), [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5815), [separate_evals.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/separate_evals.py:5841). Deleted-trace spans can now affect `total_spans`, errors, tokens, cost, start/end, while only trace summaries are filtered.

P2: Trace-item note writes can now resolve a CH root span and then write `SpanNotes` by `span_id`, but `SpanNotes.span` is still a PG FK. If the CH span has no corresponding PG `ObservationSpan`, `update_or_create()` will fail instead of simply having no PG target: [annotation_queues.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/annotation_queues.py:1262), [annotation_queues.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/annotation_queues.py:5295), [span_notes.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/tracer/models/span_notes.py:12).

P3: Root-span selection tie-break changed for trace notes. The old ORM path ordered roots by `start_time, created_at`; the CH path relies on `list_by_trace()` ordering of `start_time, id` and returns the first conversation root encountered: [annotation_queues.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/annotation_queues.py:1257), [annotation_queues.py](/Users/nikhilpareek/Documents/futureAGI/code/core/future-agi/futureagi/model_hub/views/annotation_queues.py:1267). Usually low risk, but duplicate/tied roots can attach notes to a different span.

I did not find a current `.project` / `.trace` AttributeError on CHSpan in the changed helper paths; the new shims mostly use `.id`, `.trace_id`, `.project_id`. Score upserts still include `queue_item` in the uniqueness lookup, so per-queue score isolation is preserved. I did not run tests; this was a static commit review.
